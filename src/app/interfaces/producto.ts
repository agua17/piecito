//ponemos los tipos que guardamos en la clase producto

export interface Producto {
    nombre: string
    descripcion: string
    precio: string
    talla: number[]
    urlImage: string
    genero: string
    color: string
}
